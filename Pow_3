class Solution {
public:
    bool isPowerOfThree(int n) {
        int ans;
        for (int i=0;i<=19;i++)
        {
            ans=pow(3,i);
            if(ans==n)
            return true;
        }
        return false;
    }
};
